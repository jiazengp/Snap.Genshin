<Page x:Class="DGP.Genshin.Pages.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DGP.Genshin.Pages"
      xmlns:ui="http://schemas.modernwpf.com/2019" 
      xmlns:smoothscrollviewer="clr-namespace:DGP.Genshin.Controls.SmoothScrollViewer"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HomePage" Loaded="Page_Loaded">
    <Page.Resources>
        <Style TargetType="{x:Type ui:ListViewItem}">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>
    </Page.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <smoothscrollviewer:SmoothScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Hidden">
            <ui:ListView HorizontalAlignment="Left" ItemsSource="{Binding Posts}" IsSelectionEnabled="False" Padding="0" Margin="12,0,0,0" SnapsToDevicePixels="True">
                <ui:ListView.ItemTemplate>
                    <DataTemplate>
                        <Button Tag="{Binding PostId}" Click="PostButtonClick">
                            <Button.Style>
                                <Style TargetType="{x:Type Button}">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderBrush" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="{x:Type Button}">
                                                <Border 
                                x:Name="border" 
                                CornerRadius="{DynamicResource IconCornerRadius}"
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Background="{TemplateBinding Background}" 
                                SnapsToDevicePixels="true">
                                                    <ui:ContentPresenterEx 
                                    x:Name="contentPresenter" 
                                    Focusable="False" 
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                    Margin="{TemplateBinding Padding}" 
                                    RecognizesAccessKey="True" 
                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" 
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                            <Grid Margin="0" Background="{StaticResource SystemControlBackgroundChromeMediumBrush}">
                                <Image RenderOptions.BitmapScalingMode="Fant" Source="{Binding Banner}"/>
                                <Border VerticalAlignment="Bottom" Padding="8" Background="{StaticResource SystemControlBackgroundAltMediumBrush}">
                                    <TextBlock Text="{Binding Subject}" Foreground="{StaticResource SystemControlForegroundBaseHighBrush}" TextTrimming="CharacterEllipsis"/>
                                </Border>
                                <Border VerticalAlignment="Top" HorizontalAlignment="Right" Padding="8,4" Background="{StaticResource SystemControlBackgroundAltMediumBrush}">
                                    <TextBlock Foreground="{StaticResource SystemControlForegroundBaseHighBrush}" TextTrimming="CharacterEllipsis">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock" BasedOn="{StaticResource BodyTextBlockStyle}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding OfficialType}" Value="1">
                                                        <Setter Property="Text" Value="公告"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding OfficialType}" Value="2">
                                                        <Setter Property="Text" Value="活动"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding OfficialType}" Value="3">
                                                        <Setter Property="Text" Value="资讯"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </Grid>
                        </Button>
                    </DataTemplate>
                </ui:ListView.ItemTemplate>
            </ui:ListView>
        </smoothscrollviewer:SmoothScrollViewer>
    </Grid>
</Page>
